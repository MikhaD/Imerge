export default class Imerge {
	static svg = document.querySelector("#imerge");
	/**
	 * Create new imerge 
	 * @param {Option} image - The image option from the gallery
	 * @param {Settings} settings - The settings object to construct the imerge based on
	 */
	// Another constructor to make one from another imerge? or reset function?
	constructor(image, settings) {
		// calculate initial color based on option and 
	}
}
